import { render, screen } from '@testing-library/react';
import AdminPage from '../page';

describe('AdminPage', () => {
    it.skip('renders the admin dashboard heading', () => {
        // render(<AdminPage />);
        // expect(screen.getByRole('heading', { level: 1, name: /dashboard overview/i })).toBeInTheDocument();
    });
});
